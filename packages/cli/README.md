# @plugload/cli

Command-line connection testing, schema inspection, operation previews, independent approvals, apply, and audit verification for [Plugload](https://github.com/owwix/plugload).

Approval credentials must remain outside agent runtimes. See the repository security and setup documentation before use.
